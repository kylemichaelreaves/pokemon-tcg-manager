# Database connection settings (used by backend application)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pokemon_cards
DB_USER=pokemon
DB_PASSWORD=changeme

# PostgreSQL container settings (used by docker postgres image)
POSTGRES_DB=pokemon_cards
POSTGRES_USER=pokemon
POSTGRES_PASSWORD=changeme

# Image serving
IMAGE_BASE_URL=http://localhost:3001/images
STATIC_IMAGES_DIR=./data/card-images
